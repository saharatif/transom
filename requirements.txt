fastapi>=0.115.6
uvicorn[standard]>=0.49.0
python-multipart>=0.0.32
pydantic>=2.9.0
pydantic-settings>=2.14.2
python-dotenv>=1.2.2

# LLM / AI
openai>=2.44.0
mistralai==1.5.2

# RAG
# The langchain family moved to ranges (late 0.3 line) instead of the
# original exact pins: langchain-openai 0.2.x caps openai<2.0.0, which
# conflicts with the openai>=2.44.0 pin above (needed by logfire, see
# BUGS.md #7), and langchain 0.3.7 in turn caps a langchain-core too old
# for langchain-openai 0.3.35. The dev venv happened to hold a mix pip's
# strict resolver refuses to recreate — these ranges resolve cleanly.
langchain>=0.3.27,<0.4
langchain-openai>=0.3.35,<0.4
langchain-community>=0.3.27,<0.4
langchain-pinecone==0.2.0
langgraph==0.2.45
pinecone-client==5.0.1

# PII
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.5

# Image
opencv-python==4.10.0.84
pillow==11.0.0
pymupdf==1.28.0

# Reranking
sentence-transformers==3.3.0

# Observability
logfire[fastapi]>=4.37.0
importlib_metadata>=6.0
opentelemetry-api>=1.42.1
opentelemetry-semantic-conventions>=0.63b1

# MCP
mcp>=1.28.1

# Testing (dev)
pytest>=9.1.1