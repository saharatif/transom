=====================================================================
TEST LOG — Property Intelligence System
Milestone 2 (Photo pipeline) — manual verification runs
=====================================================================

---------------------------------------------------------------------
TEST 1: preprocess.py — blur_faces_and_plates() on image with no face
---------------------------------------------------------------------
Test name: preprocess_no_face_test

Bash command:
uv run python -c "
from backend.ingestion.preprocess import blur_faces_and_plates
out = blur_faces_and_plates('docs/damaged_property/05d11f75-c19d-475e-a592-3ff32c67c5a0.jpeg')
print('output:', out)
"

Test input:
docs/damaged_property/05d11f75-c19d-475e-a592-3ff32c67c5a0.jpeg
(exterior front-entrance photo, no human face present)

Test output:
output: docs/damaged_property/05d11f75-c19d-475e-a592-3ff32c67c5a0_safe.jpeg
-> Ran without error. No faces detected (expected, since none are present),
   so output image is effectively an unmodified copy. Confirmed function
   executes cleanly end-to-end. Output file removed after test (not a real
   face case) — see Test 2 for the real face-blur verification.

---------------------------------------------------------------------
TEST 2: preprocess.py — blur_faces_and_plates() on images WITH faces
---------------------------------------------------------------------
Test name: preprocess_with_face_test

Bash command:
uv run python -c "
from backend.ingestion.preprocess import blur_faces_and_plates
import shutil, os

images = [
    'docs/damaged_property/Gemini_Generated_Image_qr4lm3qr4lm3qr4l_with_face.png',
    'docs/damaged_property/Gemini_Generated_Image_37u5hg37u5hg37u5_with_face.png',
]

for img in images:
    out = blur_faces_and_plates(img)
    dest = os.path.join('docs/preprocess_output', os.path.basename(out))
    shutil.move(out, dest)
    print(f'{img} -> {dest}')
"

Test input:
docs/damaged_property/Gemini_Generated_Image_qr4lm3qr4lm3qr4l_with_face.png
  (exterior shot, 2 people/faces visible)
docs/damaged_property/Gemini_Generated_Image_37u5hg37u5hg37u5_with_face.png
  (interior kitchen shot, 1 person/face visible)

Test output:
docs/damaged_property/Gemini_Generated_Image_qr4lm3qr4lm3qr4l_with_face.png -> docs/preprocess_output/Gemini_Generated_Image_qr4lm3qr4lm3qr4l_with_face_safe.png
docs/damaged_property/Gemini_Generated_Image_37u5hg37u5hg37u5_with_face.png -> docs/preprocess_output/Gemini_Generated_Image_37u5hg37u5hg37u5_with_face_safe.png
-> Both faces confirmed visually blurred (Gaussian blur applied over detected
   face bounding boxes) in the output PNGs. Outputs saved under
   docs/preprocess_output/.

---------------------------------------------------------------------
TEST 3: pii.py — redact_and_tokenize() / de_tokenize() round trip
---------------------------------------------------------------------
Test name: pii_redact_detokenize_roundtrip_test

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from backend.redaction.pii import redact_and_tokenize, de_tokenize
import backend.db.setup_db as setup_db
setup_db.setup_db()

text = 'Inspector John Smith can be reached at john.smith@example.com or 214-555-0199.'
clean = redact_and_tokenize(text, 'test-doc-1', setup_db.DB_PATH)
restored = de_tokenize(clean, 'test-doc-1', setup_db.DB_PATH)

with open('pii_text_output.txt', 'w') as f:
    f.write('INPUT:\n' + text + '\n\n')
    f.write('REDACTED:\n' + clean + '\n\n')
    f.write('RESTORED (de-tokenized):\n' + restored + '\n')

print('done')
"

Test input:
"Inspector John Smith can be reached at john.smith@example.com or 214-555-0199."

Test output:
REDACTED:
"Inspector [PERSON_F72B9C] can be reached at [EMAIL_ADDRESS_AC63AA] or [PHONE_NUMBER_312642]."

RESTORED (de-tokenized):
"Inspector John Smith can be reached at john.smith@example.com or 214-555-0199."
-> PERSON, EMAIL_ADDRESS, and PHONE_NUMBER all correctly detected, tokenized,
   stored in pii_token_map, and correctly restored via de_tokenize(). Full
   input/output saved to backend/redaction/pii_text_output.txt.

Note: required a separate setup step — Presidio's AnalyzerEngine() needs the
en_core_web_lg spaCy model, which was not installed by requirements.txt alone
(see BUGS.md #8).

---------------------------------------------------------------------
TEST 4: image_agent.py — analyze_property_image() end-to-end
---------------------------------------------------------------------
Test name: image_agent_vision_analysis_test

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.ingestion.image_agent import analyze_property_image
import os

result = analyze_property_image(
    'docs/damaged_property/12021852-c863-4823-90ff-ef0fc2ef99b0.jpeg',
    property_id='test-prop-1',
    doc_id='test-img-1',
    db_path=os.environ.get('DATABASE_PATH', './property_intel.db')
)
import json
print(json.dumps(result, indent=2))
"

Test input:
docs/damaged_property/12021852-c863-4823-90ff-ef0fc2ef99b0.jpeg
(interior room photo — visible ceiling hole/water damage, cracked wall,
hardwood flooring)

Test output:
{
  "floor_type": "hardwood",
  "floor_condition": "worn",
  "wood_species": "oak",
  "paint_condition": "poor",
  "condition_score": 3,
  "visible_issues": [
    "ceiling damage",
    "wall cracks",
    "water staining"
  ],
  "estimated_age_years": 15,
  "confidence": "medium"
}
-> Full pipeline verified: blur_faces_and_plates() -> GPT-4o Vision call ->
   redact_and_tokenize() -> JSON parse. Output correctly reflects visible
   damage in the source photo (ceiling hole, wall crack, worn hardwood).
   Note: this ad-hoc script did not call logfire.configure(), so this run
   was not traced in the Logfire dashboard (only backend/main.py's
   init_logfire() does that) — tracing will be verified once wired into the
   /ingest/photo endpoint.

---------------------------------------------------------------------
TEST 5: POST /ingest/photo — full endpoint, real HTTP request
---------------------------------------------------------------------
Test name: ingest_photo_endpoint_test

Bash command:
uv run uvicorn backend.main:app --port 8000 &
curl -s -X POST http://127.0.0.1:8000/ingest/photo \
  -F "property_id=test-prop-1" \
  -F "file=@docs/damaged_property/84a4b04e-d6f0-4adb-a3b0-0d0312f9c73b.jpeg"

Test input:
docs/damaged_property/84a4b04e-d6f0-4adb-a3b0-0d0312f9c73b.jpeg
(uploaded via multipart/form-data POST, property_id=test-prop-1)

Test output:
{"status":"ok","assessment":{"floor_type":"hardwood","floor_condition":"good","wood_species":"oak","paint_condition":"good","condition_score":7,"visible_issues":["cabinet damage"],"estimated_age_years":10,"confidence":"medium"}}

Logfire trace (confirmed live in dashboard):
POST /ingest/photo
  -> Calling backend.ingestion.image_agent.analyze_property_image
     -> Chat Completion with 'gpt-4o' [LLM]
     -> Calling backend.redaction.pii.redact_and_tokenize
        -> pii_detected

-> Full endpoint verified end-to-end via real HTTP request (not a direct
   Python call): file upload -> temp storage -> image agent -> Vision ->
   redaction -> JSON response. All spans confirmed nested correctly in
   Logfire, closing out both remaining Milestone 2 items.

---------------------------------------------------------------------
TEST 6: blueprint_agent.py — process_blueprint() end-to-end
---------------------------------------------------------------------
Test name: blueprint_agent_ocr_extraction_test

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.ingestion.blueprint_agent import process_blueprint
import os, json

result = process_blueprint(
    'docs/Blueprints/Blueprint_A101_Ground_Floor.pdf',
    property_id='test-prop-1',
    doc_id='test-blueprint-1',
    db_path=os.environ.get('DATABASE_PATH', './property_intel.db')
)
print(json.dumps(result, indent=2))
"

Test input:
docs/Blueprints/Blueprint_A101_Ground_Floor.pdf
(ground floor blueprint PDF)

Test output:
{
  "sqft": 2201,
  "bedrooms": null,
  "bathrooms": null,
  "rooms": [],
  "layout_type": "GROUND FLOOR"
}
-> Full pipeline verified: Mistral OCR (base64 document_url) -> join page
   markdown -> redact_and_tokenize() -> GPT-4o structured extraction ->
   JSON parse. sqft correctly extracted (2201 sqft). bedrooms/bathrooms/
   rooms came back empty/null, which is plausible since this is only the
   Ground Floor plan (A101) — bedrooms likely appear on the Upper Floor
   plan (A102). Mechanically the pipeline works end-to-end; required two
   fixes to the OCR call and response parsing (see BUGS.md #10, #11) plus
   an mistralai SDK version pin (#10) before this succeeded.

---------------------------------------------------------------------
TEST 7: inspection_parser.py — attempt 1, Mistral OCR text (FAILED)
---------------------------------------------------------------------
Test name: inspection_parser_ocr_text_attempt

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.ingestion.inspection_parser import parse_inspection_form
import os, json

result = parse_inspection_form(
    'docs/pdfs/Property_Inspection_form_filled.pdf',
    property_id='test-prop-1',
    doc_id='test-inspection-1',
    db_path=os.environ.get('DATABASE_PATH', './property_intel.db')
)
print(json.dumps(result, indent=2))
"

Test input:
docs/pdfs/Property_Inspection_form_filled.pdf
(4-page filled inspection form, handwritten checkmarks + notes)

Test output:
{
  "floor_type": "",
  "floor_condition": "",
  "wood_grade": "",
  "paint_condition": "",
  "roof_condition": "",
  "hvac_age_years": "",
  "kitchen_condition": "Cabinets need refacing / replacing",
  "bathroom_condition": "",
  "renovation_cost_estimate": "43000 - 70000",
  "overall_condition_score": ""
}
-> FAILED (mostly empty). Root cause: Mistral OCR's markdown conversion
   drops checkbox glyph state — checked and unchecked options render as
   identical plain bullet text, so GPT-4o has no way to know which was
   selected. See BUGS.md #12. Rewrote inspection_parser.py to use GPT-4o
   Vision on rendered page images instead — see Test 8.

---------------------------------------------------------------------
TEST 8: inspection_parser.py — attempt 2, GPT-4o Vision on page images (PASSED)
---------------------------------------------------------------------
Test name: inspection_parser_vision_attempt

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.ingestion.inspection_parser import parse_inspection_form
import os, json

result = parse_inspection_form(
    'docs/pdfs/Property_Inspection_form_filled.pdf',
    property_id='test-prop-1',
    doc_id='test-inspection-2',
    db_path=os.environ.get('DATABASE_PATH', './property_intel.db')
)
print(json.dumps(result, indent=2))
"

Test input:
docs/pdfs/Property_Inspection_form_filled.pdf
(same 4-page filled inspection form, rendered to page images via pymupdf)

Test output:
{
  "floor_type": "Engineered hardwood",
  "floor_condition": "Warping / cupping present",
  "wood_grade": "Mixed / inconsistent grade",
  "paint_condition": "Poor — peeling / flaking",
  "roof_condition": "Fair — granule loss / aging",
  "hvac_age_years": "11–15 years",
  "kitchen_condition": "Cabinets need refacing / replacing",
  "bathroom_condition": "Grout / caulk deteriorating",
  "renovation_cost_estimate": "Roof replacement: 8000-10000 USD, HVAC system: 5000-10000 USD, Kitchen remodel: 9000-18000 USD, Bathroom updates: 5000-10000 USD, Flooring refinish: [PHONE_NUMBER_ADECE3] USD, Exterior / curb appeal: [PHONE_NUMBER_ADECE3] USD, Paint (interior): 1000-3000 USD",
  "overall_condition_score": null
}
-> PASSED (all checkbox fields correctly extracted, verified against manual
   visual read of the PDF). Two known limitations found and documented:
   (a) renovation_cost_estimate has two entries misredacted as
   PHONE_NUMBER by Presidio (false positive on digit-hyphen-digit cost
   ranges) — investigated a score_threshold fix, rejected because it
   scores identically to real contextless phone numbers (see BUGS.md #13,
   accepted as open trade-off); (b) overall_condition_score came back
   null — the handwritten circled score on page 1 was not confidently
   extracted (genuine scan-quality/ambiguity issue, not a code bug).

---------------------------------------------------------------------
TEST 9: POST /ingest/blueprint — full endpoint, real HTTP request
---------------------------------------------------------------------
Test name: ingest_blueprint_endpoint_test

Bash command:
uv run uvicorn backend.main:app --port 8000 &
curl -s -X POST http://127.0.0.1:8000/ingest/blueprint \
  -F "property_id=test-prop-1" \
  -F "file=@docs/Blueprints/Blueprint_A102_Upper_Floor.pdf"

Test input:
docs/Blueprints/Blueprint_A102_Upper_Floor.pdf
(uploaded via multipart/form-data POST, property_id=test-prop-1)

Test output:
{"status":"ok","fields":{"sqft":null,"bedrooms":4,"bathrooms":3,"rooms":[],"layout_type":"upper floor plan"}}

Logfire trace (confirmed live in dashboard):
POST /ingest/blueprint
  -> Calling backend.ingestion.blueprint_agent.process_blueprint
     -> Pydantic Unmarshaller/Marshaller validate_python (OCR request/response)
     -> Calling backend.redaction.pii.redact_and_tokenize
        -> pii_detected
     -> Chat Completion with 'gpt-4o' [LLM]

-> Full endpoint verified end-to-end. Correctly extracted bedrooms=4,
   bathrooms=3 from the Upper Floor plan — complements Test 6 (Ground
   Floor plan, which had sqft but no bedroom/bathroom info), confirming
   the two blueprint pages partition property info as expected. sqft null
   here is expected (total sqft is on the Ground Floor sheet, A101).

---------------------------------------------------------------------
TEST 10: POST /ingest/inspection — full endpoint, real HTTP request
---------------------------------------------------------------------
Test name: ingest_inspection_endpoint_test

Bash command:
uv run uvicorn backend.main:app --port 8000 &
curl -s -X POST http://127.0.0.1:8000/ingest/inspection \
  -F "property_id=test-prop-1" \
  -F "file=@docs/pdfs/Property_Inspection_form_filled.pdf"

Test input:
docs/pdfs/Property_Inspection_form_filled.pdf
(uploaded via multipart/form-data POST, property_id=test-prop-1)

Test output:
{"status":"ok","fields":{"floor_type":"Engineered hardwood","floor_condition":"Water damage visible","wood_grade":"Mixed / inconsistent grade","paint_condition":"Poor — peeling / flaking","roof_condition":"Fair — granule loss / aging","hvac_age_years":"11-15 years","kitchen_condition":"Cabinets need refacing / replacing","bathroom_condition":"Grout / caulk deteriorating","renovation_cost_estimate":[{"item":"Roof replacement","priority":1,"est_cost":"8000 - 10000 USD","roi":"140%"},{"item":"HVAC system","priority":1,"est_cost":"5000 - 10000 USD","roi":"120%"},{"item":"Kitchen remodel","priority":2,"est_cost":"9000 - 18000 USD","roi":"150%"},{"item":"Bathroom updates","priority":2,"est_cost":"5000 - 10000 USD","roi":"125%"},{"item":"Flooring refinish","priority":2,"est_cost":"[PHONE_NUMBER_759A4A] USD","roi":"142%"},{"item":"Exterior / curb appeal","priority":1,"est_cost":"[PHONE_NUMBER_759A4A] USD","roi":"150%"},{"item":"Paint (interior)","priority":2,"est_cost":"1000 - 3000 USD","roi":"152%"}],"overall_condition_score":null}}

Logfire trace (confirmed live in dashboard):
POST /ingest/inspection
  -> Calling backend.ingestion.inspection_parser.parse_inspection_form
     -> Chat Completion with 'gpt-4o' [LLM]
     -> Calling backend.redaction.pii.redact_and_tokenize
        -> pii_detected

-> Full endpoint verified end-to-end via real HTTP request. This time the
   model additionally structured renovation_cost_estimate as a JSON array
   per line item (rather than the single string from Test 8), still
   reproducing the known PHONE_NUMBER false positive on "4000-9000" and
   "3000-7000" cost ranges (BUGS.md #13, accepted open trade-off).
   overall_condition_score again null (BUGS.md — scan-quality limitation).
   Closes out Milestone 3.

---------------------------------------------------------------------
TEST 11: calculator.py — worked examples from texas_property_valuation_formulas.pdf
---------------------------------------------------------------------
Test name: valuation_calculator_worked_examples_test

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from backend.valuation.calculator import calculate_base_value, calculate_renovation_impact, ROI_TABLE

base = calculate_base_value(sqft=2000, year_built=2006, price_per_sqft=180, current_year=2026)
print('Base value:', base, '| expected 288000.0 | match:', base == 288000.0)

renovations = [
    {'category': 'roof_replacement', 'cost': 10000},
    {'category': 'kitchen_remodel', 'cost': 12000},
]
result = calculate_renovation_impact(base, renovations, local_median_ppsf=180, sqft=2000)
print('Updated value:', result['updated_value'], '| expected 320000.0 | match:', result['updated_value'] == 320000.0)
print('Net equity gain:', result['net_equity_gain'], '| expected 10000.0 | match:', result['net_equity_gain'] == 10000.0)
print('Total uplift:', result['total_value_uplift'], '| expected 32000.0')
print('Ceiling applied:', result['ceiling_applied'], '| expected False (ceiling far above)')

expected_multipliers = {
    'roof_replacement': 1.40, 'hvac_replacement': 1.20, 'kitchen_remodel': 1.50,
    'bathroom_updates': 1.25, 'flooring_refinish': 1.42, 'curb_appeal_exterior': 1.50,
}
mismatches = [k for k, v in expected_multipliers.items() if ROI_TABLE[k]['multiplier'] != v]
print('ROI table multiplier mismatches:', mismatches, '| expected []')
"

Test input:
Section 1.3 worked example: 2000 sqft, built 2006, current year 2026,
$180/sqft, standard maintenance (default depreciation_rate=0.010)
Section 3 worked example: same property + roof replacement ($10,000) +
kitchen remodel ($12,000)
ROI_TABLE checked against Section 2.1's exact multiplier table

Test output:
Base value: 288000.0 | expected 288000.0 | match: True
Updated value: 320000.0 | expected 320000.0 | match: True
Net equity gain: 10000.0 | expected 10000.0 | match: True
Total uplift: 32000.0 | expected 32000.0
Ceiling applied: False | expected False (ceiling far above)
ROI table multiplier mismatches: [] | expected []

-> All exact worked-example figures from
   docs/pdfs/texas_property_valuation_formulas.pdf reproduced precisely:
   base value $288,000, updated value $320,000, net equity gain $10,000.
   All six ROI_TABLE multipliers verified against the source doc's
   Section 2.1 table (roof 1.40x, HVAC 1.20x, kitchen 1.50x, bathroom
   1.25x, flooring 1.42x, curb appeal 1.50x).

   Note: the doc's third worked example (all six renovations completed:
   total cost $48,500, total uplift $67,000, 138% blended ROI) was not
   used for an exact-match test — the doc gives only the aggregate
   totals, not each category's individual renovation cost, so there's no
   unique cost split to reverse-engineer and test against. Verified the
   formula mechanics instead via the ceiling test below.

---------------------------------------------------------------------
TEST 12: calculator.py — neighborhood ceiling cap
---------------------------------------------------------------------
Test name: valuation_calculator_ceiling_cap_test

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from backend.valuation.calculator import calculate_base_value, calculate_renovation_impact

base = calculate_base_value(sqft=1200, year_built=2015, price_per_sqft=150, current_year=2026)
renovations = [
    {'category': 'kitchen_remodel', 'cost': 18000},
    {'category': 'bathroom_updates', 'cost': 12000},
    {'category': 'roof_replacement', 'cost': 14000},
]
result = calculate_renovation_impact(base, renovations, local_median_ppsf=150, sqft=1200)
ceiling = 150 * 1200 * 1.20
print('Base value:', base)
print('Raw updated (uncapped) would be:', base + result['total_value_uplift'])
print('Ceiling:', ceiling)
print('Updated value (capped):', result['updated_value'])
print('Ceiling applied:', result['ceiling_applied'], '| expected True')
print('Updated value == ceiling:', result['updated_value'] == ceiling)
"

Test input:
1200 sqft, built 2015, $150/sqft, current year 2026, three renovations
totaling $44,000 cost (kitchen $18k, bathroom $12k, roof $14k) — an
intentionally oversized renovation spend relative to the property to
force the raw updated value above the neighborhood ceiling.

Test output:
Base value: 160200.0
Raw updated (uncapped) would be: 221800.0
Ceiling: 216000.0
Updated value (capped): 216000.0
Ceiling applied: True | expected True
Updated value == ceiling: True

-> Confirms the neighborhood ceiling check from Section 2 ("Updated Value
   should not exceed roughly 1.15-1.25x local median price/sqft x sqft")
   works correctly: raw updated value ($221,800) exceeds the ceiling
   ($216,000 at the default 1.20x multiplier), so the capped value is
   returned instead and ceiling_applied is correctly flagged True.
   Closes out Milestone 4.

---------------------------------------------------------------------
TEST 13: rag/ingestion.py — ingest_document() against real warranty PDF
---------------------------------------------------------------------
Test name: rag_ingestion_warranty_test

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.rag.ingestion import ingest_document
import os, json

result = ingest_document(
    'docs/pdfs/Warranty_documents.pdf',
    property_id='test-prop-1',
    doc_id='test-warranty-1',
    db_path=os.environ.get('DATABASE_PATH', './property_intel.db'),
    doc_type='warranty'
)
print(json.dumps(result, indent=2))
"

Test input:
docs/pdfs/Warranty_documents.pdf (34-page TAB Express Limited Home
Warranty document)

Test output (first attempt):
ValueError: No active indexes found in your Pinecone project
-> Pinecone index "property-intelligence" didn't exist yet. Created it
   via pinecone-client (dimension=1536 for text-embedding-3-small,
   metric=cosine, ServerlessSpec aws/us-east-1), polled until ready.

Test output (after creating index):
{
  "chunks": 229,
  "namespace": "property_test-prop-1_warranty"
}
-> Ingestion pipeline verified: PyMuPDFLoader -> redact_and_tokenize() ->
   structural chunking -> OpenAI embeddings -> Pinecone upsert. 229 chunks
   created and stored successfully.

---------------------------------------------------------------------
TEST 14: rag/graph.py — rag_agent full LangGraph pipeline (attempt 1, FAILED then FIXED)
---------------------------------------------------------------------
Test name: rag_agent_end_to_end_attempt

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.rag.graph import rag_agent

result = rag_agent.invoke({
    'question': 'Is a 1/8 inch crack in the master bath drywall covered under warranty?',
    'property_id': 'test-prop-1',
    'doc_type': 'warranty',
    'documents': [],
    'is_relevant': False,
    'answer': '',
    'citations': [],
    'iterations': 0
})
print(result['answer'])
print(result['citations'])
"

Test input:
Question: 'Is a 1/8 inch crack in the master bath drywall covered under
warranty?' against the ingested Warranty_documents.pdf (same question as
the original implementation doc's demo script, Module 4 Section 17)

Test output (first run):
ValueError: 'answer' is already being used as a state key
-> Node named "answer" collided with the RAGState.answer field (langgraph
   0.2.45, matches the pin exactly). Renamed node to "generate_answer" in
   graph.py (see BUGS.md #14).

Test output (second run, after node rename):
ANSWER: Yes, a 1/8 inch crack in the master bath drywall is covered under
warranty. According to the warranty document, a drywall surface shall not
crack such that any crack equals or exceeds 1/32 of an inch in width...
(Section 2). Since 1/8 inch is greater than 1/32 inch, it would be covered.
CITATIONS: ['Section 2']
DOCS RETRIEVED: 3

-> Answer content was substantively correct, but citation ("Section 2")
   was WRONG — verified via full-text search of the source PDF that the
   real clause is under "§ 7. Performance Standards for Drywall". Further
   investigation (see BUGS.md #15) found none of the 3 retrieved chunks
   even contained the "1/32 of an inch" clause — the answer was not
   actually grounded in retrieval despite the "ONLY the sources" system
   prompt. Root causes: (a) cross-encoder reranker demoted the correct
   chunk out of top_n=3 due to a lexical false-match with an unrelated
   stucco clause also mentioning "1/8 of an inch"; (b) chunks carried no
   section-number metadata, so even a correctly retrieved chunk couldn't
   be reliably cited. Fixed in ingestion.py (section metadata tagging),
   retrieval.py (widened k=5->6, top_n=3->4), and graph.py (answer_node
   now cites real section tags). See Test 15 for the fix verification.

---------------------------------------------------------------------
TEST 15: rag_agent — same question, after grounding/reranking fixes
---------------------------------------------------------------------
Test name: rag_agent_end_to_end_verified

Bash command:
# Cleared and re-ingested the warranty namespace with section metadata:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from pinecone import Pinecone
import os
pc = Pinecone(api_key=os.environ['PINECONE_API_KEY'])
pc.Index('property-intelligence').delete(delete_all=True, namespace='property_test-prop-1_warranty')
"
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.rag.ingestion import ingest_document
import os
ingest_document('docs/pdfs/Warranty_documents.pdf', 'test-prop-1', 'test-warranty-2',
    os.environ.get('DATABASE_PATH', './property_intel.db'), 'warranty')
"
# Then re-ran the same rag_agent.invoke() call as Test 14

Test input:
Same question: 'Is a 1/8 inch crack in the master bath drywall covered
under warranty?'

Test output:
ANSWER: [§ 7. Performance Standards for Drywall]
A drywall surface shall not crack such that any crack equals or exceeds
1/32 of an inch in width at any point along the length of the crack. A
1/8 inch crack exceeds this standard, so it would typically be covered by
warranty if it falls within the warranty period.
CITATIONS: ['§ 7']
DOCS RETRIEVED: 4
 - § 11. Performance Standards for Stucco | has 1/32: False
 - § 7. Performance Standards for Drywall | has 1/32: True
 - § 3. Express Limited Home Warranty | has 1/32: False
 - § 16. Performance Standards for Countertops and Backsplashes | has 1/32: False

-> PASSED. Citation now correctly reads "§ 7" (verified against source PDF
   via full-text search earlier). The chunk containing the actual "1/32 of
   an inch" clause is now present among retrieved documents (was absent
   in Test 14's first run). Confirms both fixes worked: section metadata
   tagging grounds citations in real data, and widened k/top_n prevents
   the reranker from silently discarding the correct chunk. Closes out
   Milestone 5's core RAG pipeline (LangChain ingestion, retriever +
   reranker, LangGraph agent). Residual risk documented in BUGS.md #15 —
   no automated check yet verifies a citation's section actually appears
   in the retrieved context for arbitrary future questions.

---------------------------------------------------------------------
TEST 16: SQLite persistence gap — end-to-end re-verification (Milestone 6 prep)
---------------------------------------------------------------------
Test name: sqlite_persistence_end_to_end_test

Bash command:
rm -f property_intel.db
/Users/saharatif/transom/.venv/bin/python3 backend/db/setup_db.py
uv run uvicorn backend.main:app --port 8000 &
curl -s -X POST http://127.0.0.1:8000/ingest/photo -F "property_id=1" \
  -F "file=@docs/damaged_property/12021852-c863-4823-90ff-ef0fc2ef99b0.jpeg"
curl -s -X POST http://127.0.0.1:8000/ingest/blueprint -F "property_id=1" \
  -F "file=@docs/Blueprints/Blueprint_A101_Ground_Floor.pdf"
curl -s -X POST http://127.0.0.1:8000/ingest/blueprint -F "property_id=1" \
  -F "file=@docs/Blueprints/Blueprint_A102_Upper_Floor.pdf"
curl -s -X POST http://127.0.0.1:8000/ingest/inspection -F "property_id=1" \
  -F "file=@docs/pdfs/Property_Inspection_form_filled.pdf"

Test input:
Fresh DB, one property (id=1), all 4 real test documents run through
their endpoints in sequence.

Test output:
All 4 requests returned 200 OK with expected extracted JSON. Direct
SQLite query afterward confirmed:
properties: {id=1, sqft=2201, bedrooms=4, bathrooms=3.0, builder=null,
  year_built=null, ...}  (before inspection_parser.py fix — see below)
property_images: 1 row (condition_score=2, issues_detected=[...])
material_assessment: 2 rows (source='ai_photo', source='inspection')
inspection_forms: 1 row

-> PASSED for basic persistence (properties row correctly merges sqft
   from Ground Floor with bedrooms/bathrooms from Upper Floor via
   COALESCE). Found this was previously entirely missing (BUGS.md #17) —
   none of the /ingest/* endpoints wrote to SQLite before this fix, only
   returned JSON to the caller. Also required changing property_id from
   str to int on all three endpoints to match properties.id's
   INTEGER PRIMARY KEY schema type.

---------------------------------------------------------------------
TEST 17: inspection_parser.py — extended to capture PROPERTY DETAILS header
---------------------------------------------------------------------
Test name: inspection_parser_property_details_test

Bash command:
curl -s -X POST http://127.0.0.1:8000/ingest/inspection -F "property_id=1" \
  -F "file=@docs/pdfs/Property_Inspection_form_filled.pdf"
# (after extending the extraction prompt to also request builder,
# year_built, address, city_state_zip)

Test input:
Same filled inspection form, re-run after prompt extension.

Test output:
{"builder":"Texas Homes","year_built":2006,"address":null,
"city_state_zip":null, ...condition fields unchanged...}

properties row after re-ingest:
{'id': 1, 'address': None, 'city_state_zip': None, 'builder': 'Texas Homes',
 'year_built': 2006, 'sqft': 2201, 'bedrooms': 4, 'bathrooms': 3.0, ...}

-> PASSED. builder and year_built correctly extracted and persisted via
   save_inspection_form()'s new COALESCE UPDATE into properties.
   address/city_state_zip correctly came back null — confirmed earlier
   (when the filled PDF was first reviewed) that those fields are
   genuinely blank on the physical form, not an extraction miss.
   Discovered this gap because calculate_renovation_roi (Test 18) needs
   year_built and failed with a TypeError until this was fixed.

---------------------------------------------------------------------
TEST 18: mcp/server.py — all 6 MCP tools against real persisted data
---------------------------------------------------------------------
Test name: mcp_tools_end_to_end_test

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
import json
from backend.mcp.server import (get_property_summary, get_maintenance,
    estimate_property_value, calculate_renovation_roi, get_contractors,
    get_warranty_coverage)
print(json.dumps(get_property_summary(1), indent=2))
print(json.dumps(get_maintenance(1), indent=2))
print(json.dumps(estimate_property_value(sqft=2201, year_built=2006, zip_code='75001'), indent=2))
print(json.dumps(calculate_renovation_roi(1, [
    {'category': 'roof_replacement', 'cost': 10000},
    {'category': 'kitchen_remodel', 'cost': 12000}], zip_code='75001'), indent=2))
print(json.dumps(get_contractors('Kitchen & Bathroom Updates'), indent=2))
print(json.dumps(get_warranty_coverage(1, 'Is a 1/8 inch crack in the master bath drywall covered under warranty?'), indent=2))
"

Test input:
property_id=1 (real, persisted data from Tests 16-17); warranty doc
re-ingested under namespace property_1_warranty (was previously under a
stale property_id='test-prop-1' string namespace, mismatched with the
new integer property_id convention)

Test output:
get_property_summary(1): full row incl. sqft=2201, bedrooms=4, bathrooms=3,
  builder='Texas Homes', year_built=2006
get_maintenance(1): [] (correctly empty — maintenance_needs never populated
  by any ingestion pipeline; out of current scope)
estimate_property_value: {"base_value": 316944.0, "price_per_sqft": 180.0}
  (2201 * 180 * age_factor(0.80 for 20yr @2026) = 316,944 -- matches formula)
calculate_renovation_roi(1): {"base_value": 316944.0,
  "total_renovation_cost": 22000, "total_value_uplift": 32000.0,
  "updated_value": 348944.0, "net_equity_gain": 10000.0,
  "ceiling_applied": false, "breakdown": [...]}
get_contractors('Kitchen & Bathroom Updates'):
  [{"id": 2, "company_name": "Spruced - Decorating Den", ...}]
get_warranty_coverage(1, ...): {"answer": "[§ 7. Performance Standards for
  Drywall] A drywall surface shall not crack such that any crack equals or
  exceeds 1/32 of an inch...", "citations": ["§ 7"]}

-> ALL 6 TOOLS PASSED against real, end-to-end persisted/ingested data.
   mcp==1.1.0 had no FastMCP API at all (ModuleNotFoundError) — upgraded
   to mcp==1.28.1 (BUGS.md #16), which also pulled in unrelated transitive
   upgrades (uvicorn, python-dotenv, python-multipart, pydantic-settings)
   that were verified not to break anything (app still boots, /health
   still 200 OK). get_warranty_coverage's first attempt (before
   re-ingesting under the correct namespace) returned "not covered" with
   no citations — correct, safe behavior given no sources existed for
   that property_id's namespace at the time, not a bug in the tool
   itself. Closes out Milestone 6.

---------------------------------------------------------------------
TEST 19: Frontend — visual verification via headless Chromium (Playwright)
---------------------------------------------------------------------
Test name: frontend_dashboard_visual_verification_test

Bash command:
uv run uvicorn backend.main:app --port 8000 &
cd frontend && npm run dev &
# then a Playwright script (chromium.launch --no-sandbox):
#   nav http://localhost:5173, wait for "Property Dashboard",
#   screenshot light mode, click theme toggle, screenshot dark mode,
#   capture console errors

Test input:
Fresh dashboard load, property_id=1 (real data from Tests 16-18: sqft,
bedrooms, bathrooms, builder, year_built, renovation breakdown all
populated in SQLite from earlier milestones).

Test output (attempt 1):
CONSOLE_ERRORS: ["Failed to load resource: net::ERR_CONNECTION_RESET",
  "TypeError: Failed to fetch at getProperty ..."]
-> /property/1 fetch failed from the browser (and from plain Node fetch)
   despite curl succeeding against the same URL. Root cause: "localhost"
   resolves to ::1 (IPv6) before 127.0.0.1 on this system; uvicorn only
   binds IPv4. Fixed by changing frontend/src/api/client.js's API base
   URL to http://127.0.0.1:8000 (BUGS.md #18).

Test output (attempt 2, after IPv4 fix):
CONSOLE_ERRORS: []
-> Data loaded, but screenshot showed PropertyCard's title/price/address/
   badges section completely missing between the photo thumbnails and
   the Renovation Priorities panel. Inspected the live DOM directly
   (page.locator('.property-card').innerHTML()) — the correct markup
   WAS present ("Property #1", "4 Bed", "3 Bath", "2,201 sqft", "Texas
   Homes", "Built 2006", etc.) — not a data bug. Measured element:
   clientHeight=186 vs scrollHeight=399 — content clipped by
   overflow:hidden due to flexbox default shrink behavior (BUGS.md #19).
   Fixed by adding flex-shrink:0 to .property-card and .reno-table-panel.

Test output (attempt 3, after flex-shrink fix):
CONSOLE_ERRORS: []
-> Full dashboard renders correctly: sidebar nav, theme toggle, 3-column
   layout (Upload | Property Card + Renovation Table | Chat), real
   property photos (hero + 3 thumbnails), full property details (title,
   price, address, bed/bath/sqft badges, builder/year, Available status
   pin), renovation table with 7 rows including looked-up contractors
   (Power HRG, Kohler Home Energy, Spruced - Decorating Den, EFS
   Flooring and Remodeling) via the CATEGORY_MAP normalization, and the
   chat panel with its initial greeting. Verified in both light and dark
   themes — colors/contrast match design/DESIGN.md's token table in both
   modes. Screenshots: /tmp/dashboard_light.png, /tmp/dashboard_dark.png.

---------------------------------------------------------------------
TEST 20: Frontend — real chat interaction end-to-end
---------------------------------------------------------------------
Test name: frontend_chat_interaction_test

Bash command:
# Playwright script: fill the chat input, click send, wait for the
# "Thinking…" bubble to disappear (up to 40s — RAG grade/rewrite loop
# can take ~20s), screenshot.
page.fill('input[aria-label="Message"]', 'Is a 1/8 inch crack in the
  master bath drywall covered under warranty?')
page.click('button[aria-label="Send message"]')
page.waitForSelector('text=Thinking…', {state: 'hidden', timeout: 40000})

Test input:
Same warranty question used throughout Milestone 5/6 testing, typed into
the live chat UI (not called directly via Python).

Test output:
Client message bubble (blue, right-aligned) appeared immediately. Agent
bubble (light blue, left-aligned) appeared after ~15-20s with the answer
text and a "§ 7" citation chip rendered below it, matching the
ChatPanel's citation-chip design. Zero console errors throughout.

-> PASSED. Full loop verified working end-to-end through the actual UI:
   type message -> POST /chat -> RAG agent -> rendered answer + citation
   chip. One finding: this run's final answer concluded "No, not
   covered," while the earlier Milestone 5 test (same question, same
   correct § 7 citation) concluded "would be covered" — the model's
   yes/no interpretation is not stable across runs even though the
   grounding/citation itself is correct both times. Logged as an open
   limitation (BUGS.md #20), not fixed. Closes out Milestone 7's
   frontend build and integration testing.

---------------------------------------------------------------------
TEST 21: rag_agent — measurement-comparison consistency fix (BUGS.md #20)
---------------------------------------------------------------------
Test name: rag_agent_measurement_reasoning_consistency_test

Bash command:
/Users/saharatif/transom/.venv/bin/python3 -c "
from backend.rag.measurement import extract_inch_measurements, compare_measurement_to_sources
q = 'Is a 1/8 inch crack in the master bath drywall covered under warranty?'
src = '(c) A drywall surface shall not crack such that any crack equals or exceeds 1/32 of an inch in width...'
print(extract_inch_measurements(q))
print(extract_inch_measurements(src))
print(compare_measurement_to_sources(q, src))
"
# then, after wiring into graph.py's answer_node:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.rag.graph import rag_agent
for i in range(5):
    result = rag_agent.invoke({'question': 'Is a 1/8 inch crack in the master bath drywall covered under warranty?', 'property_id': '1', 'doc_type': 'warranty', 'documents': [], 'is_relevant': False, 'answer': '', 'citations': [], 'iterations': 0})
    print(result['answer'][:150])
"
# negative-case check:
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.rag.graph import rag_agent
result = rag_agent.invoke({'question': 'Is a 1/64 inch crack in the master bath drywall covered under warranty?', 'property_id': '1', 'doc_type': 'warranty', 'documents': [], 'is_relevant': False, 'answer': '', 'citations': [], 'iterations': 0})
print(result['answer'])
"

Test input:
Unit test: extract_inch_measurements()/compare_measurement_to_sources()
against the real question + real source clause text.
Integration test: same "1/8 inch crack" question run 5 times through the
full rag_agent pipeline (real retrieval, real GPT-4o calls).
Negative case: "1/64 inch crack" (below the 1/32 inch threshold, should
conclude NOT covered).

Test output:
Unit test:
  question values: [0.125]
  source values: [0.03125]
  comparison: {'measured_inches': 0.125, 'threshold_inches': 0.03125,
    'exceeds_threshold': True}
-> Correct deterministic comparison confirmed before wiring into graph.py.

5-run consistency test: all 5 runs retrieved the threshold chunk
(has_threshold_chunk=True) AND all 5 concluded "covered/defect"
consistently — e.g. "A 1/8 inch crack in the drywall exceeds the
allowable crack width of 1/32 of an inch, so it would be considered
[a covered defect]." Zero flip-flops across 5 runs (previously
inconsistent per BUGS.md #20's original finding).

Negative case output:
"A 1/64 inch crack in the drywall is not covered under warranty because
it does not meet the threshold for coverage... Since 1/64 inch is less
than 1/32 inch, it does not qualify for repair under the performance
standards." citations: ['§ 7']
-> Correctly concluded NOT covered when the measurement is within
   tolerance — confirms the fix applies the comparison correctly in
   both directions, not just biased toward "covered."

-> PASSED. Fix (BUGS.md #20) verified: (1) measurement.py's
   extract_inch_measurements()/compare_measurement_to_sources() do the
   numeric comparison deterministically in Python; (2) graph.py's
   answer_node injects the result as a COMPUTED FACT plus an explicit
   two-step reasoning rule, removing the model's freely-interpreted
   connection between "exceeds threshold" and "is covered." Note: one
   separate run outside this 5-run batch still returned "not covered"
   with zero citations due to a retrieval miss (the threshold chunk
   wasn't retrieved that time) — a pre-existing, different issue
   (BUGS.md #15 residual risk), not a regression or failure of this fix,
   since the reasoning fix only applies once the correct source is
   actually in context.

---------------------------------------------------------------------
TEST 22: Live dashboard use — two real data-quality regressions found and fixed
---------------------------------------------------------------------
Test name: live_dashboard_regression_fix_test

Context: user re-uploaded Blueprint_A102_Upper_Floor.png and the filled
inspection form through the running frontend (not a scripted test) and
noticed the PropertyCard badges changed from "4 Bed / 3 Bath" to
"2 Bed / 1 Bath", and the Renovation Priorities table went completely
blank (category/cost/ROI/contractor all empty or "—").

--- Bug A: blueprint bedroom/bathroom instability (BUGS.md #21) ---

Bash command (reproduction):
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.ingestion.blueprint_agent import process_blueprint
result = process_blueprint('docs/Blueprints/Blueprint_A102_Upper_Floor.png',
    'test-prop-1', 'test-mime-bug', 'property_intel.db')
print(result)
"

Test output (reproduction, before fix):
{"sqft": null, "bedrooms": 2, "bathrooms": 1,
 "rooms": ["Bedroom 3", "Bedroom 4", "Bath 3"], "layout_type": "Upper Floor Plan"}
-> Reproduced exactly. Compared raw Mistral OCR markdown between the
   .pdf and .png versions of this sheet directly -- identical text in
   both: "BEDROOMS 3 & 4 + BATH 3". Ruled out the mime-type theory as
   the actual cause of THIS discrepancy (though still fixed as a real,
   separate bug -- mimetypes.guess_type() now sets the correct mime
   type instead of always assuming application/pdf).
   Real cause: GPT-4o's extraction step interpreted the ambiguous
   "BEDROOMS 3 & 4" label differently across runs -- literal item count
   (2 bed) vs. highest room number in a numbering sequence (4 bed).

Bash command (after prompt fix -- explicit room-numbering instruction):
/Users/saharatif/transom/.venv/bin/python3 -c "
from dotenv import load_dotenv
load_dotenv()
from backend.ingestion.blueprint_agent import process_blueprint
for i in range(3):
    result = process_blueprint('docs/Blueprints/Blueprint_A102_Upper_Floor.png',
        'test-prop-1', f'test-numbering-fix-{i}', 'property_intel.db')
    print(f'run {i+1}: bedrooms={result[\"bedrooms\"]}, bathrooms={result[\"bathrooms\"]}')
"

Test output (after fix):
run 1: bedrooms=4, bathrooms=3
run 2: bedrooms=4, bathrooms=3
run 3: bedrooms=4, bathrooms=3
-> PASSED. 3/3 consistent, correct results.

--- Bug B: renovation_cost_estimate shape instability (BUGS.md #22) ---

Test input:
Same filled inspection form re-parsed 3 times after tightening the
renovation_cost_estimate prompt to one fixed JSON schema.

Test output (all 3 runs, abbreviated):
[
  {"category": "Roof replacement", "priority": 1, "cost": "8000-10000 USD", "roi": "140%"},
  {"category": "HVAC system", "priority": 1, "cost": "5000-10000 USD", "roi": "120%"},
  {"category": "Kitchen remodel", "priority": 2, "cost": "9000-18000 USD", "roi": "150%"},
  ...
  {"category": "Other", "priority": null, "cost": null, "roi": null}
]
-> PASSED. All 3 runs returned the exact same fixed shape
   (category/priority/cost/roi keys) -- no more shape drift. Known,
   separate PHONE_NUMBER false-positive on cost ranges (BUGS.md #13)
   still occasionally appears in the "cost" field -- unrelated to this
   fix, already an accepted open trade-off.

--- Live re-verification ---

Bash command:
curl -s -X POST http://127.0.0.1:8000/ingest/blueprint -F "property_id=1" \
  -F "file=@docs/Blueprints/Blueprint_A102_Upper_Floor.png"
curl -s -X POST http://127.0.0.1:8000/ingest/inspection -F "property_id=1" \
  -F "file=@docs/pdfs/Property_Inspection_form_filled.pdf"
curl -s http://127.0.0.1:8000/property/1
# + Playwright screenshot of the live dashboard

Test output:
/property/1 correctly shows bedrooms=4, bathrooms=3.0, and a fully
populated renovations array (8 rows including "Other"). Screenshot
confirmed PropertyCard badges read "4 Bed / 3 Bath" and Renovation
Priorities table shows all rows with category, priority chip, cost,
ROI, and contractor populated -- matching pre-regression appearance.

-> Both regressions found via real interactive use (not a scripted
   test) were root-caused, fixed, and verified for consistency across
   multiple runs, then confirmed live in the running dashboard.

---------------------------------------------------------------------
TEST 23: Four user-requested fixes — logo, refresh button, painter
contractor, estimated_value wiring
---------------------------------------------------------------------
Test name: dashboard_polish_and_valuation_wiring_test

Requests:
1. Use the actual logo from design/Gemini_..._logo.png instead of plain
   "Transom" text in the header.
2. Add a process/refresh button so uploads or stale data can be
   manually re-checked.
3. Add contractor "That 1 Painter" (https://that1painter.com/dallas,
   Richardson TX, (214) 432-4636) to renovation_companies.
4. Explain (and fix) why PropertyCard always showed "Value pending".

Bash command (contractor insert):
/Users/saharatif/transom/.venv/bin/python3 -c "
import sqlite3
conn = sqlite3.connect('property_intel.db')
conn.execute(
    'INSERT INTO renovation_companies (category, company_name, website, location, phone) VALUES (?, ?, ?, ?, ?)',
    ('Interior Painting', 'That 1 Painter', 'https://that1painter.com/dallas', 'Richardson, TX', '(214) 432-4636')
)
conn.commit()
"

Bash command (estimated_value fix test):
/Users/saharatif/transom/.venv/bin/python3 -c "
from backend.db.queries import _maybe_update_estimated_value, get_property
_maybe_update_estimated_value(1)
print(get_property(1))
"

Test output:
{'id': 1, ..., 'sqft': 2201, 'year_built': 2006, ..., 'price_per_sqft': 180.0,
 'estimated_value': 316944.0, ...}
-> Matches the earlier direct MCP estimate_property_value() test result
   exactly (Test 18). Confirms _maybe_update_estimated_value() computes
   correctly.

Live verification (Playwright screenshot against running dashboard):
- Header shows the actual Transom logo image (icon + wordmark), not
  plain text.
- A refresh button (circular arrow, spin animation while loading) sits
  next to the theme toggle; clicking it calls refreshProperty().
- FileUpload's doc-type caption reads "Property Photo — multiple files
  allowed" for photo/blueprint tabs (from the earlier multi-file work),
  confirming that change is also live.
- PropertyCard header shows "$316,944" instead of "Value pending".
- RenovationTable's "Paint (interior)" row now shows "That 1 Painte[r]"
  as its contractor (previously "—", since no category mapped to
  paint work at all) -- confirmed via CATEGORY_MAP addition
  ('paint (interior)' -> 'Interior Painting').
- Zero console errors throughout.

-> PASSED. All 4 requested items implemented, verified against the
   live running app (not just unit-level), and documented (BUGS.md #23
   for the estimated_value gap; schema.sql updated for the painter
   contractor so fresh setups get it automatically going forward).

---------------------------------------------------------------------
TEST 24: SVG logo recreation (light/dark) + theme-swap wiring
---------------------------------------------------------------------
Test name: svg_logo_theme_swap_test

Request: make the header logo bigger, recreate it as SVG for light and
dark mode, save the SVGs in design/.

Bash command (initial color sampling from the source PNG):
/Users/saharatif/transom/.venv/bin/python3 -c "
from PIL import Image
img = Image.open('design/Gemini_Generated_Image_a1qcbsa1qcbsa1qc_logo.png').convert('RGB')
# sampled icon band -> teal ~ (2,84,108); text band -> charcoal ~ (61,61,61)
"

Created: design/transom-logo-light.svg, design/transom-logo-dark.svg
(three ascending column rects + diagonal beam polygon + "TRANSOM" text,
colors tied to this app's own theme.css tokens: light icon #02546C /
text #212529; dark icon #00CFFF / text #E0E0E0), copied into
frontend/public/, wired into App.vue with height increased 40px->56px.

Test output (attempt 1 -- CSS-only theme swap via :global()):
Playwright screenshot showed the DARK-mode screenshot still displaying
the light-colored icon/text (muted navy + near-invisible dark text on
dark background) -- swap not happening.
page.evaluate() check:
  { dataTheme: 'dark', lightDisplay: 'block', darkDisplay: 'none' }
-> The :global([data-theme='dark']) .logo-light CSS rule silently
   didn't compile -- inspected actual injected stylesheet, rule was
   missing entirely (BUGS.md #25).

Test output (attempt 2 -- after fixing dark SVG's XML comment, CSS
swap still in place):
img.naturalWidth: 0 for the dark variant -- broken image, separate
issue from the swap logic. Found <!-- ... --color-primary ... --> in
the dark SVG's comment contains a literal "--" sequence, which is
invalid inside an XML comment; browser's XML parser fatally rejected
the whole SVG document when loaded via <img src> (BUGS.md #24).

Fix applied: (a) reworded the dark SVG comment to avoid any "--"
sequence; (b) replaced the CSS :global() swap with a MutationObserver
in App.vue watching <html data-theme>, driving isDarkMode ref ->
v-if/v-else between the two <img> tags.

Test output (attempt 3, after both fixes):
dark naturalWidth: 255 (real decoded image)
Screenshots confirmed: light mode shows teal icon (#02546C) + dark
wordmark on white; dark mode shows bright cyan icon (#00CFFF,
matching --color-primary dark token) + light wordmark (#E0E0E0) on
dark background. Toggling the theme button live swaps the image
immediately in both directions.

-> PASSED after two real bugs found and fixed (BUGS.md #24, #25).
   Removed the now-unused frontend/public/logo.png (PNG fallback from
   the earlier request, superseded by the SVG pair).

---------------------------------------------------------------------
TEST 25: Fresh-start state (no value shown until real uploads)
---------------------------------------------------------------------
Test name: fresh_database_no_value_test

Context: user asked why $316,944 appeared as a "fixed" value on app
start and wanted no value shown until uploads actually happen.
Explanation (not a bug): $316,944 = calculate_base_value(sqft=2201
from the blueprint, year_built=2006 from the inspection form,
$180/sqft) via _maybe_update_estimated_value() (BUGS.md #23) -- it
IS real, upload-derived data, just left over in property_intel.db
from earlier testing sessions. App.vue hardcodes propertyId=1, and
that property's data persists across server restarts since SQLite is
a real file on disk -- restarting the app doesn't reset data.

Bash command:
rm -f property_intel.db
/Users/saharatif/transom/.venv/bin/python3 backend/db/setup_db.py
/Users/saharatif/transom/.venv/bin/python3 -c "
import sqlite3
conn = sqlite3.connect('property_intel.db')
for table in ['properties', 'property_images', 'material_assessment', 'inspection_forms', 'renovation_companies']:
    print(table, ':', conn.execute(f'SELECT COUNT(*) FROM {table}').fetchone()[0], 'rows')
"

Test output:
properties : 0 rows
property_images : 0 rows
material_assessment : 0 rows
inspection_forms : 0 rows
renovation_companies : 5 rows
-> renovation_companies correctly retains its 5 seeded contractors
   (reference data, re-seeded by schema.sql on every setup_db.py run,
   not upload-derived) -- everything else correctly empty.

GET /property/1 (after reset): {"property":null,"renovations":[]}

Live verification (Playwright screenshot):
PropertyCard shows "No property data yet -- upload photos, a
blueprint, or an inspection form to get started." (no title, no price,
no badges). RenovationTable shows "No renovation data yet...". Upload
History shows "No uploads yet this session." No console errors.

-> PASSED. Confirmed the "fixed value" was real persisted data from
   earlier sessions, not a bug or hardcoded placeholder. Database reset
   to a clean state per user's request so property #1 now genuinely
   starts empty and only gets a value after real blueprint + inspection
   form uploads. Note: hero/thumbnail photos in PropertyCard are static
   design-reference images (design/DESIGN.md spec), not tied to actual
   uploaded photos -- still show even in the empty state; flagged to
   user as a known, separate scope item, not changed without a request.

---------------------------------------------------------------------
TEST 26: pii.py currency-context filter (fixes BUGS.md #13) + blueprint
0-vs-null bug (BUGS.md #27)
---------------------------------------------------------------------
Test name: currency_context_filter_and_blueprint_null_fix_test

Context: user pointed out the RenovationTable showing
"[PHONE_NUMBER_735CF1] USD" for Flooring refinish / Exterior curb
appeal, and proposed a fix: "If the number has 'USD' or dollar sign
next to it ... then its cost. Otherwise consider it as phone number."

Bash command (filter unit test):
/Users/saharatif/transom/.venv/bin/python3 -c "
from backend.redaction.pii import analyzer
text = 'Flooring refinish: 4000-9000 USD, John called 214-555-0199'
results = analyzer.analyze(text=text, language='en')
for r in results:
    print(r.entity_type, repr(text[r.start:r.end]), '| next:', repr(text[r.end:r.end+10]))
"

Test output:
PHONE_NUMBER '4000-9000' | next: ' USD, John'
PHONE_NUMBER '214-555-0199' | next: ''
-> Confirmed the pattern: false positive has ' USD' immediately after,
   real phone number doesn't. Implemented _looks_like_currency() regex
   check in pii.py, filtering PHONE_NUMBER matches before tokenizing.

Bash command (fix verification, both directions):
/Users/saharatif/transom/.venv/bin/python3 -c "
import importlib
import backend.redaction.pii as pii
importlib.reload(pii)
print(pii.redact_and_tokenize('Flooring refinish: 4000-9000 USD, Exterior / curb appeal: 3000-7000 USD', 'test-currency-fix', 'property_intel.db'))
print(pii.redact_and_tokenize('Inspector John Smith can be reached at 214-555-0199 or john.smith@example.com', 'test-realphone-fix', 'property_intel.db'))
"

Test output:
Currency test: Flooring refinish: 4000-9000 USD, Exterior / curb appeal: 3000-7000 USD
Real phone test: Inspector [PERSON_2FE334] can be reached at [PHONE_NUMBER_9A9067] or [EMAIL_ADDRESS_ED0DAA]
-> PASSED both directions: currency ranges no longer redacted, real
   phone number still correctly redacted.

Full parse_inspection_form() re-run against the real filled form:
all 7 renovation_cost_estimate entries returned clean dollar ranges
("4000-9000 USD", "3000-7000 USD", etc.) with zero PHONE_NUMBER
placeholders.

--- Second issue found while re-testing live: blueprint 0-vs-null ---

Re-ingesting Ground Floor blueprint through the live API (server still
running OLD pii.py in memory -- module caching, no --reload) surfaced:
{"sqft":2201,"bedrooms":0,"bathrooms":0,...} -- properties.bedrooms/
bathrooms dropped from correct 4/3 to 0/0, since COALESCE(0, existing)
treats 0 as real data and overwrites. Root cause: the #21 room-
numbering prompt fix caused GPT-4o to sometimes return 0 instead of
null for "no bedroom info on this sheet."

Fix: (1) blueprint_agent.py prompt now explicitly says use null, not 0,
when a sheet has no room info; (2) save_blueprint_fields() changed to
COALESCE(NULLIF(?, 0), existing) as a persistence-layer safeguard,
given this field's repeat-incident history (#21, this one).

Bash command (manual repair + full restart + re-verification):
/Users/saharatif/transom/.venv/bin/python3 -c "
import sqlite3
conn = sqlite3.connect('property_intel.db')
conn.execute('UPDATE properties SET bedrooms = 4, bathrooms = 3 WHERE id = 1')
conn.commit()
"
# restarted backend to load pii.py + blueprint_agent.py + queries.py fixes
curl -s -X POST http://127.0.0.1:8000/ingest/blueprint -F "property_id=1" \
  -F "file=@docs/Blueprints/Blueprint_A101_Ground_Floor.pdf"
curl -s -X POST http://127.0.0.1:8000/ingest/inspection -F "property_id=1" \
  -F "file=@docs/pdfs/Property_Inspection_form_filled.pdf"
curl -s http://127.0.0.1:8000/property/1

Test output:
Ground Floor re-ingest: {"sqft":2201,"bedrooms":null,"bathrooms":null,...}
-> correctly null this time, not 0.
/property/1: bedrooms=4, bathrooms=3.0 (preserved from Upper Floor,
NOT clobbered), sqft=2201, estimated_value=316944.0.
Renovation breakdown: all 7 cost entries clean, no PHONE_NUMBER
placeholders.

Live verification (Playwright screenshot): PropertyCard shows "4 Bed",
"3 Bath", "2,201 sqft", "$316,944". RenovationTable shows real dollar
costs across all rows with contractors correctly resolved.

-> PASSED. Two real bugs found and fixed in one session: a
   user-proposed, well-targeted PII redaction fix (#26, resolves the
   long-standing #13 open item), and a fresh regression caught before
   it could compound further (#27) thanks to re-testing immediately
   after applying the first fix.

---------------------------------------------------------------------
TEST 27: New downloaded logo assets wired in, resized bigger
---------------------------------------------------------------------
Test name: new_logo_assets_test

Request: user downloaded two new logo images (design/transom-logo-
light.svg.png, design/transom-logo-dark.svg.png -- PNG files despite
the .svg.png name) and asked to use them, bigger than before.

Bash command (content-bbox detection to crop excess canvas whitespace):
/Users/saharatif/transom/.venv/bin/python3 -c "
from PIL import Image
import numpy as np
for name, f in [('light','design/transom-logo-light.svg.png'), ('dark','design/transom-logo-dark.svg.png')]:
    img = Image.open(f).convert('RGB')
    arr = np.array(img).astype(int)
    bg = arr[5,5]
    dist = np.sqrt(((arr - bg) ** 2).sum(axis=2))
    mask = dist > 25
    ys, xs = np.where(mask)
    print(name, 'bbox: x', xs.min(), xs.max(), 'y', ys.min(), ys.max())
"

Test output: both images 1600x1000 canvas, content bbox x:496-1097
y:100-730 (identical layout in both variants). Cropped with 30px
padding -> 661x690 design/transom-logo-{light,dark}-full.png, copied to
frontend/public/, wired into App.vue (replacing the earlier hand-drawn
SVGs), height increased 56px -> 84px, rounded-corner card treatment
kept since these PNGs have their own near-white/near-black background
(not transparent) rather than exactly matching theme.css tokens.

Live verification (Playwright):
naturalWidth 661 for both light and dark variants (loaded correctly).
Screenshots confirm: bigger logo showing the gradient teal/cyan
ascending-columns icon, "TRANSOM" wordmark, and "ARCHITECTURAL
STRUCTURES" tagline, correctly swapping between light and dark
versions via the existing MutationObserver-driven v-if (BUGS.md #25).
Full dashboard screenshot confirms no layout regressions elsewhere.
Zero console errors.

-> PASSED. Old hand-drawn placeholder SVGs (transom-logo-{light,dark}.svg)
   removed from frontend/public/ (kept in design/ as historical
   reference, superseded by these higher-fidelity downloaded assets).

---------------------------------------------------------------------
TEST 28: Dashboard UX/UI upgrade to premium-mockup reference image
---------------------------------------------------------------------
Test name: dashboard_premium_redesign_test

Request: user shared a screenshot of a fuller "premium SaaS dashboard"
mockup (top bar w/ search + org selector, labeled sidebar nav, tabbed
upload queue w/ progress bars, sortable/filterable tables, chat with
suggested-question chips) and asked to upgrade the app's UX/UI to match.

Scoping decision (confirmed with user before building): visual polish
only, not a full rebuild — skip nav items/features that don't map to
anything real (Portfolio, Clients, Reports, Organization Selector,
multi-tenant); keep/restyle what does. Also confirmed: default theme
switched from light to dark to match the mockup.

Changes made:
- ThemeToggle.vue + index.html: default theme now dark (was light).
  Added an inline <script> in index.html that sets data-theme before
  Vue mounts, avoiding a flash-of-wrong-theme on load.
- Sidebar.vue: widened 64px -> 200px, icon+label nav rows instead of
  icon-only. Nav items trimmed to only what's real: Dashboard, Upload,
  Chat, Settings (dropped Clients, which was a non-functional
  placeholder in the original build too).
- App.vue: added a top bar with a "Jump to property ID" search input
  (functional -- Enter submits and calls getProperty for that ID,
  triggering a refetch), a "Property #N" pill, kept refresh + theme
  toggle. Logo resized 84px -> 52px to fit a slim single-row top bar
  alongside the search input.
- FileUpload.vue: renamed "Upload a New Property" panel to "Asset
  Ingestion Queue"; added "Active Queue" / "Upload History" tabs over
  the same underlying upload-history list (Active Queue = items still
  Processing, filtered live); added an animated indeterminate progress
  bar per in-flight upload row (real per-byte progress isn't available
  from fetch(), so this is an honest "something is happening" indicator
  rather than a fake percentage).
- PropertyCard.vue: added a ribbon header ("PROPERTY #1 · location")
  above the hero photo; added a "Quick Actions" row with 3 REAL actions
  (not decorative): refresh (emits to App.vue's refreshProperty),
  open primary photo in a new tab, copy property ID to clipboard
  (shows a transient \"Copied!\" confirmation).
- RenovationTable.vue: column headers (Category/Priority/Est. Cost/ROI)
  are now clickable sort toggles (ascending/descending, parses leading
  numbers out of cost/ROI strings for numeric sort); added a live
  filter-by-category text input; Contractor column upgraded from plain
  text to an actual <select> populated with the full contractor list
  for that category (was previously just showing contractors[0]).
- ChatPanel.vue: added a row of suggested-question chips above the
  input bar, using 4 REAL questions already verified against the
  actual ingested warranty document during earlier testing (not
  placeholder copy) -- clicking one fills the input and sends
  immediately.

Bash command (functional smoke tests via Playwright):
await page.click('button:has-text(\"Est. Cost\")')  // sort
await page.fill('input[aria-label=\"Filter renovations\"]', 'roof')  // filter
const optionsCount = await select.locator('option').count()  // dropdown
await page.click('.suggested-chip:has-text(\"Are plumbing leaks covered?\")')  // chip

Test output:
- Sort by Est. Cost ascending -> first row \"Paint (interior)\" (lowest
  leading cost, 1000-3000 USD). Correct.
- Filter \"roof\" -> 1 row (only \"Roof replacement\"). Correct.
- Contractor dropdown for row 1 -> 1 option (matches single seeded
  contractor for that category). Renders correctly even with one option.
- Suggested chip click -> sent \"Are plumbing leaks covered?\", got a
  real grounded response citing \"§ 28. Performance Standards for
  Plumbing Delivery Systems\" and \"§ 25. Performance Standards for
  Plumbing\". Full round trip confirmed working, not just UI wiring.

Live verification (Playwright screenshots, both themes, populated with
real re-ingested data): dark mode (new default) and light mode both
render correctly -- top bar, labeled sidebar, tabbed queue panel,
property ribbon + quick actions, sortable/filterable renovation table
with contractor dropdowns, chat with suggested chips. Zero console
errors across all screenshots and interaction tests.

-> PASSED. Note: one relative-path gotcha hit while re-ingesting test
   data for verification -- curl commands run from frontend/ as cwd
   failed silently on relative file paths (same class of issue as
   earlier sessions); fixed by using $(pwd)-qualified absolute paths.
   Not a bug in the app itself.

=====================================================================
TEST 29: Robustness pass — new pytest unit suite (66 tests)
---------------------------------------------------------------------
Test name: unit_test_suite

Covers (tests/unit/):
- test_valuation.py: PDF worked example ($288,000 base value), age-factor
  floor at 0.60, uplift/net-equity/ceiling math, all 6 ROI categories,
  unknown-category and bad-cost/sqft ValueError paths (new validation).
- test_measurement.py: fraction/decimal extraction, zero-denominator
  edge (found a real latent bug: "1/0 inch" leaked a spurious 0.0-inch
  decimal match — fixed), and the new subject-scoped threshold narrowing
  replicating the real mixed-threshold retrieval from BUGS.md #33.
- test_llm_json.py: fenced/preambled/nested/escaped JSON extraction and
  the empty/no-JSON/unbalanced/malformed error paths of the new shared
  parse_llm_json() helper.
- test_pii.py: two-distinct-people round trip (regression for BUGS.md
  #28 — previously both names restored as the SAME person), email/phone
  round trip, currency-range non-redaction (#13/#26), no-PII passthrough.
- test_queries.py: NULLIF zero-clobber guard (#27), estimated-value
  trigger firing only when sqft+year_built both exist (#23), inspection
  header COALESCE behavior, all 3 renovation_cost_estimate JSON shapes
  (#22) — against a fresh schema.sql temp DB per test.
- test_api.py: FastAPI TestClient with mocked agents — photo persistence
  stores the _safe blurred path (regression for BUGS.md #32), blueprint/
  inspection persistence, 415 extension rejection per doc type, agent
  failure -> 502 with reason and no partial DB rows, /property 404,
  blank chat 422.

Bash command:
uv run pytest tests/unit -q

Test output:
66 passed, 7 warnings in ~5s
-> PASSED (after fixing the two real bugs the suite caught: the
   measurement 1/0-inch edge and the save_photo_assessment wiring).

=====================================================================
TEST 30: Hardened API endpoints — live negative-path checks
---------------------------------------------------------------------
Test name: api_negative_paths_live_test

Bash commands:
curl -s -X POST http://127.0.0.1:8000/ingest/photo -F property_id=1 \
  -F file=@evil.txt
curl -s -X POST http://127.0.0.1:8000/ingest/inspection -F property_id=1 \
  -F 'file=@evil.txt;filename=../../etc/cron.pdf'
curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8000/property/999
curl -s -X POST http://127.0.0.1:8000/chat -F property_id=1 \
  -F 'message=   ' -o /dev/null -w '%{http_code}'

Test output:
- .txt photo upload -> 415 {"detail":"Unsupported file type .txt for
  photo. Allowed: .jpeg, .jpg, .png, .webp"}
- traversal filename '../../etc/cron.pdf' -> spooled to a safe uuid temp
  path (/var/folders/.../transom_<uuid>.pdf), then clean 502 with the
  PyMuPDF reason; no path escape, no partial DB writes.
- /property/999 -> 404 (was previously 200 with null property).
- blank chat message -> 422.
-> PASSED.

=====================================================================
TEST 31: RAG correctness + stability after graph overhaul (BUGS.md #33)
and full UI E2E of the redesigned frontend
---------------------------------------------------------------------
Test name: rag_consistency_and_ui_e2e_test

Bash commands:
for run in 1 2 3 4 5; do curl -s -X POST http://127.0.0.1:8000/chat \
  -F property_id=1 -F "message=Is a 1/8 inch crack in the drywall covered?"; done
curl ... -F "message=Is a 1/64 inch crack in the drywall covered?"
curl ... -F "message=Does the warranty cover my car parked in the garage?"
node e2e_check.mjs / e2e_chat2.mjs  (Playwright: real photo upload via
the focused Upload view, toast + history verification, suggested-chip
chat round trip, shared-session check between views, screenshots of
dashboard dark/light + upload/chat/settings views + not-found state)

Test output:
- Drywall 1/8": 5/5 runs -> covered, COMPUTED FACT cites 0.125 > 0.03125,
  citation "§ 7". (Before the fix: consistently "not covered" with no
  citations — see BUGS.md #33 for the three-layer cause.)
- Drywall 1/64": within tolerance -> not covered. Correct direction.
- Off-topic (car): "not covered". Correct.
- UI E2E: real photo upload -> success toast ("processed — property data
  updated"), Upload History row, property_images DB row storing the
  _safe blurred path with condition_score. Chat chip -> grounded § 7
  answer rendered with pre-wrap formatting + citation chip. Dashboard
  chat column shows the same transcript as the focused Chat view
  (shared-session composables). Screenshots verified in both themes and
  all four sidebar views; SVG icons render everywhere (no emoji).
-> PASSED.

=====================================================================
TEST 32: Reset-database button (Settings) — POST /reset
---------------------------------------------------------------------
Test name: reset_database_button_test

Bash commands:
uv run pytest tests/unit -q   # includes new
  test_reset_clears_ingested_data_but_keeps_contractors
node reset_check.mjs          # Playwright: Settings -> click Reset
                              # (arms) -> click again (confirms)
sqlite3 property_intel.db "SELECT COUNT(*) FROM properties; ..."

Test output:
- Unit suite: 67 passed. Reset deletes properties/property_images/
  material_assessment/maintenance_needs/inspection_forms/documents/
  pii_token_map; renovation_companies (seeded contractors) survives;
  /property/1 returns 404 afterwards.
- Live UI: first click arms the button ("Click again to confirm",
  filled red, alert icon); second click fires POST /reset -> success
  toast "Database reset — upload fresh documents to start over.";
  upload history and chat transcript reset to their initial state;
  dashboard shows the fresh "No property with this ID yet" card.
- DB after reset: properties=0, images=0, inspections=0, contractors=5.
  Pinecone warranty namespace intentionally untouched so /chat keeps
  working without re-ingesting the warranty PDF.
-> PASSED.

=====================================================================
TEST 33: New SVG brand lockup + header reset button
---------------------------------------------------------------------
Test name: brand_logo_and_header_reset_test

Changes under test:
- BrandLogo.vue: inline-SVG "transom fanlight" mark + TRANSOM /
  PROPERTY INTELLIGENCE wordmark, colored entirely by CSS theme
  variables (mark bg = primary, lines = card bg) so it recolors on
  theme change with no asset swapping. Replaces the PNG pair in the
  header (and the MutationObserver + v-if/v-else swap machinery that
  existed only to switch between them). Sidebar's "T" badge replaced
  with the same mark. Standalone deliverables saved to
  design/transom-lockup-{light,dark}.svg (no "--" inside XML comments,
  per BUGS.md #24).
- Header reset button (user couldn't find the Settings-only one):
  trash icon between refresh and the theme toggle, same two-step
  confirm handler as the Settings row (arm -> "Confirm reset" label ->
  4s auto-disarm).

Bash command:
node logo_reset_check.mjs   # Playwright, both themes

Test output:
- Dark + light screenshots: mark and wordmark render crisply in both,
  sidebar and header marks match, no page errors.
- Header reset: present on dashboard, arms to "Confirm reset" on first
  click, auto-disarms after 4s (verified label returns to icon-only).
  Confirm intentionally NOT clicked (live DB now holds the user's
  freshly re-uploaded property data).
-> PASSED.

=====================================================================
TEST 34: Frontend typeface matched to the brand (Inter Variable)
---------------------------------------------------------------------
Test name: brand_font_test

Change under test: theme.css always DECLARED Inter first in the body
font stack, but nothing ever loaded the font — every user silently got
their system fallback. Installed @fontsource-variable/inter (self-
hosted, bundled by Vite, no external font CDN requests), imported it in
main.js, and updated the stack to 'Inter Variable'. Added
font-variant-numeric: tabular-nums to tables and the price so figures
align in columns. Side effect handled: `npm install` pruned the ad-hoc
playwright package (it was never in package.json) — reinstalled as a
proper devDependency.

Bash command:
node font_check.mjs   # Playwright: document.fonts inspection

Test output:
- computed body font-family: "Inter Variable"
- document.fonts: latin subset status "loaded" (other unicode subsets
  registered, loaded on demand)
- document.fonts.check('13px "Inter Variable"') -> true (actually
  rendering with Inter, not a fallback)
- Screenshot confirms the UI, wordmark, and data tables share one
  typeface; numerals in the renovation table align.
-> PASSED.

=====================================================================
TEST 35: Session-gated dashboard (empty on page load, data only after
session activity)
---------------------------------------------------------------------
Test name: session_gated_display_test

Change under test (user request): every page load starts with an empty
dashboard, even when the database holds data from earlier sessions.
Property data appears only after activity in the CURRENT session — a
successful upload, or explicitly searching a property ID. Nothing is
deleted on refresh (an accidental F5 must not destroy uploads; the DB
and MCP tools keep everything); the header trash button remains the way
to actually clear stored data. Implemented as a sessionActive flag in
App.vue: no fetch on mount, refreshProperty() no-ops until the session
is active, manual refresh before activity shows an explanatory toast,
and a successful reset returns the session to its idle state.

Bash command:
node session_gate_check.mjs   # Playwright, DB pre-loaded with data

Test output:
1. Fresh load with populated DB -> 0 renovation rows, card shows
   "No property data yet — upload photos, a blueprint, ...".
2. Refresh-arrow click before any activity -> toast "Fresh session —
   upload documents (or search a property ID) to load data", still empty.
3. Search jump to "1" -> full data appears (8 renovation rows, $316,944).
4. F5 -> empty dashboard again.
5. sqlite3 check afterwards: DB untouched (1 property, value 316944).
-> PASSED.

=====================================================================
TEST 36: PropertyCard shows only uploaded photos (static reference
imagery removed)
---------------------------------------------------------------------
Test name: uploaded_photos_only_test

Changes under test:
- PropertyCard's hardcoded hero + 3 thumbnails (design-reference stock
  photos in public/images/) removed. The card now renders the property's
  actual uploaded photos: first upload = hero, rest = thumbnails, and a
  "No photos uploaded yet" placeholder when the property has none.
- Backend: photo pipeline now moves the blurred _safe copy into a
  persistent UPLOADS_DIR (./uploads, gitignored) instead of leaving it
  in the OS temp dir; /property/{id} now returns an images[] list
  (filtered to files that still exist, so rows pointing at reaped temp
  files degrade to the placeholder instead of broken <img> tags); new
  GET /images/{id} serves the file (404 for unknown/missing).

Bash command:
uv run pytest tests/unit -q   # 68 passed (2 new: served-image round
                              # trip; missing-file hidden + 404)
node photo_card_check.mjs     # Playwright with a real upload

Test output:
- Fresh session: zero <img> elements in the card (no static photos).
- Real photo upload -> success toast -> hero <img> src =
  http://127.0.0.1:8000/images/1, decoded (naturalWidth > 0) — i.e. the
  actual uploaded (blurred) photo served from the API, not an asset.
-> PASSED.

=====================================================================
TEST 37: Per-document-type session gating (photos-only session must not
reveal stored renovation/blueprint data)
---------------------------------------------------------------------
Test name: per_doctype_session_gate_test

Change under test (user report): with the Test 35 session gate, ANY
upload revealed the property's full stored view — uploading only photos
surfaced renovation priorities (old inspection data) and bed/bath/value
(old blueprint/inspection data) from previous sessions. Gating is now
per doc type via a sessionDocTypes set in App.vue: photos unlock only
the photo carousel; renovation priorities require an inspection upload
this session; structured details (badges/price/builder/status) require
a blueprint or inspection upload this session, with an explanatory hint
shown instead. An explicit property-ID search remains the "show me
everything stored" escape hatch, and a reset clears the gate state.

Bash command:
node doc_gate_check.mjs   # Playwright, real photo upload

Test output:
- Photo-only session: hero photo shown; 0 price / 0 badge elements;
  card hint "Upload a blueprint or inspection form to see size, rooms,
  valuation and renovation priorities."; renovation table shows its
  empty state (0 rows).
- Explicit search for "1": full view unlocks — $316,944, 3 badges,
  8 renovation rows.
-> PASSED.

=====================================================================
TEST 38: Photo tiles scoped to the current session's uploads
---------------------------------------------------------------------
Test name: session_scoped_photo_tiles_test

Change under test (user report): photos accumulate per property in
property_images across sessions, so uploading 3 photos showed 8+ tiles
(this session's 3 plus every earlier upload). POST /ingest/photo now
returns the property_images row id it created (save_photo_assessment
returns lastrowid, threaded through _run_ingestion); App.vue collects
these ids in a sessionImageIds set and passes PropertyCard a filtered
images list — only this session's uploads render, in upload order.
Explicit property-ID search still shows the full stored photo set
(consistent with the rest of the per-doc-type gating); reset clears
the id set.

Bash command:
uv run pytest tests/unit -q   # 68 passed (image_id assertion added)
node session_photos_check.mjs # Playwright, real upload

Test output:
- Fresh session, ONE photo uploaded -> exactly 1 tile (hero, 0
  thumbnails), despite 10 stored photos in the DB.
- Explicit search "1" -> full stored set appears (hero + 9 thumbnails).
-> PASSED.

=====================================================================
TEST 39: Blueprint/inspection gating split + upload doc-type race fix
---------------------------------------------------------------------
Test name: split_gating_and_doctype_race_test

Changes under test (user report: photos + one blueprint uploaded, yet
renovation priorities and the $316,944 valuation appeared):
1. showDetails split into showSpecs (beds/baths/sqft, unlocked by a
   blueprint OR inspection upload this session) and showValuation
   (price, address, builder/year — all inspection-derived — unlocked
   only by an inspection upload). Renovations already required an
   inspection. Explicit property-ID search still unlocks everything.
2. Real race bug fixed in FileUpload.vue: the emitted docType was read
   from the reactive chip AFTER the multi-second upload await — if the
   user switched Photo/Blueprint/Inspection chips while an upload was
   processing, the completion was attributed to the wrong doc type
   (mislabeled history rows AND wrongly unlocked gated panels). The doc
   type is now captured once when the upload batch starts.

Bash command:
node blueprint_gate_check.mjs   # Playwright, real blueprint upload

Test output (fresh session, ONE blueprint uploaded):
- Spec badges: 3 (4 Bed / 3 Bath / 2,201 sqft) — blueprint data shown.
- Price elements: 0; builder/meta rows: 0 (inspection-derived, hidden).
- Renovation rows: 0, with "upload a filled inspection form" empty state.
- Card hint: "Upload a filled inspection form to see valuation, builder
  details and renovation priorities."
- Photo strip shows the "No photos uploaded yet" placeholder (no photos
  this session).
-> PASSED.

=====================================================================
TEST 40: Column titles aligned on one baseline
---------------------------------------------------------------------
Test name: column_title_alignment_test

Change under test (user request): the three column headers used three
different treatments at three vertical positions — a floating uppercase
label (upload), an in-card ribbon (property), and a two-line caption +
h2 (chat). Added a shared .column-title class in theme.css (text-xs,
700, 0.06em tracking, uppercase, muted) applied to all three; the chat
header restructured to a single header bar mirroring the property
ribbon's padding/background (property # moved to the bar's right side);
the upload heading given an 11px top offset to match the in-card bars'
border + padding.

Bash command:
node align_check.mjs   # Playwright boundingBox measurement

Test output:
"ASSET INGESTION QUEUE" -> y=101.0 h=17.0
"PROPERTY"              -> y=101.0 h=17.0
"WARRANTY ASSISTANT"    -> y=101.0 h=17.0
All three identical position, height, and style. Screenshot confirms.
-> PASSED.
